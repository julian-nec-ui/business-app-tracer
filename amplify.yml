version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "MONGODB_URI=$MONGODB_URI" >> .env.local
        - echo "MONGODB_DB=$MONGODB_DB" >> .env.local
        - echo "BETTER_AUTH_SECRET=$BETTER_AUTH_SECRET" >> .env.local
        - echo "BETTER_AUTH_URL=$BETTER_AUTH_URL" >> .env.local
        - npm run build
        - npm run start
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - ./next/cache
